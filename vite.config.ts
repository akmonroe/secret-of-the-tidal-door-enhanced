import { defineConfig } from "vite";

// Relative base so the build works on GitHub Pages and CDN mirrors.
// Live URL once Pages is on: https://akmonroe.github.io/secret-of-the-tidal-door/
export default defineConfig({
  base: "./",
});
